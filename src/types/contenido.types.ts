export type CarouselSlide = {
  title: string;
  description: string;
  image: string;
  label: string;
};

export type TukuyModule = {
  icon: string;
  title: string;
  description: string;
  tags: string[];
};
